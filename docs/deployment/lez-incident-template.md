# LEZ Explorer Reconciliation Incident

- Observed at UTC:
- Reporter and source commit:
- LEZ release and channel ID:
- Sequencer URL, health, head ID/hash/status/timestamp:
- Newest sequencer finalized ID/hash:
- Explorer URL and latest finalized ID/hash/timestamp:
- Candidate transaction hash/type:
- Candidate block ID/hash/finality:
- Exact transaction URL result:
- Exact block URL result:
- Matching overlap block IDs/hashes:
- Indexer lag in finalized blocks, or `unknown`:
- Classification: `pending-indexer`, `stalled-indexer`, `divergent-chain`,
  `reset-or-pruning`, `reorg`, `wrong-endpoint`, `invalid-encoding`, or
  `unresolved-upstream`:
- Sanitized reproduction command/output reference:
- Secret scan result:
- Recheck time and outcome:

Do not attach seeds, ISKs, private keys, mnemonic phrases, plaintext payloads,
private proofs, encrypted account material, or complete private wallet history.
