[package]
name = "bonded-inbox-program"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[lib]
crate-type = ["lib"]

[profile.release]
overflow-checks = true
panic = "abort"
lto = true
codegen-units = 1
